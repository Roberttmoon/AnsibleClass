prefix: table3-rm
location: francecentral
group_name: "{{ prefix }}-awesomeapp"
vnet: "{{ prefix }}-vnet"
security_group: "{{ prefix }}-secgroup"
subnet: "{{ prefix }}-subnet"
nic: "{{ prefix }}-nic"
pubip: "{{ prefix }}-publicip"
